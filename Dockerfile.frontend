# ── 前端开发环境 — npm 依赖预装，源码通过 volume 挂载 ──
FROM node:22-alpine

WORKDIR /app/frontend

COPY frontend/package*.json ./
RUN npm install

# 不 COPY 源码 — 通过 volume 挂载实现热重载
EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
