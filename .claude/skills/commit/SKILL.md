# Commit Skill
1. Run `git diff --stat` to review all changes
2. Run `git diff` to check for any debug code or leftover comments
3. Ask user for commit message, or generate one from the changes
4. Run `git add -A && git commit -m "<message>"`
5. Confirm commit success
